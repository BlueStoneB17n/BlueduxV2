# macOS
.DS_Store

# Google OAuth client credentials (downloaded from GCP Console)
client_secret_*.json

# Local sftpgo source clone (independent repo, has its own .git)
sftpgo/

# sftpgo runtime data dump - imported once via /api/v2/loaddata, lives in Postgres after that
deploy/sftpgo-railway/events-bootstrap.json

# mise tool versions are environment-specific to this machine
mise.toml
