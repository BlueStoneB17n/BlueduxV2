# macOS
.DS_Store

# Google OAuth client credentials (downloaded from GCP Console)
client_secret_*.json

# Local sftpgo source clone (independent repo, has its own .git)
sftpgo/

# sftpgo runtime data dump - imported once via /api/v2/loaddata, lives in Postgres after that
deploy/sftpgo-railway/events-bootstrap.json
deploy/sftpgo-railway/events-webhook.json

# mise tool versions are environment-specific to this machine
mise.toml

# Node / pnpm
node_modules/
.next/
.turbo/
dist/
*.tsbuildinfo
*.log

# Drizzle migrations cache
.drizzle/

# Local env files
.env
.env.local
.env.*.local

# Auth0 credential notes (tenant domain + client IDs/secrets) — ROOT only.
# fact/auth0.md is configuration docs without credentials, do commit it.
/auth0.md

# Per-developer Claude Code session memo (each contributor maintains their own)
CLAUDE.md

